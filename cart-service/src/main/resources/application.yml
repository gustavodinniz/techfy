server:
  port: 8081
spring:
  application:
    name: ProductService
  datasource:
    url: jdbc:postgresql://localhost:5432/techfy-db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update